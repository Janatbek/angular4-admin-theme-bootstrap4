import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DataPolicyTestingComponent } from './data-policy-testing.component';

describe('DataPolicyTestingComponent', () => {
  let component: DataPolicyTestingComponent;
  let fixture: ComponentFixture<DataPolicyTestingComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DataPolicyTestingComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DataPolicyTestingComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
